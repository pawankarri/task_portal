spring:
  application:
    name: leave-service
  datasource:
    url: jdbc:mysql://localhost:3306/task_portal
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8091
